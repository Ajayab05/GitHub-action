name: Runner Test

on:
workflow_dispatch:

jobs:
test:
runs-on: self-hosted

steps:
  - name: Check Runner
    run: |
      hostname
      whoami
      docker --version
      aws --version
